# Guardrail Vars
skip_host: false

# Vars below configured by orchestrator "begin_patch.py"
host_list: []
region: "{{ region | default('') }}"

# Folder generated by orchestrator "${HOME}/patch-{yyyymmdd-hhmmss}/"
setup_dir: "{{ lookup('env', 'HOME') }}/patch"
setup_vars_dir: "{{ setup_dir }}/vars"
